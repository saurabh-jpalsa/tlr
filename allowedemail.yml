name: allowedemail
version: 0.0.1
patches:
  common-env-features: |
    # restrict registration by email pattern
    "REGISTRATION_EMAIL_PATTERNS_ALLOWED": ["^.*@ifmr\\.ac\\.in$", "(^\\w+\\.\\w+)@povertyactionlab\\.org$"]
